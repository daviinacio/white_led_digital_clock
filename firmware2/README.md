# White Led Digital Clock : Firmware v2

## Commands

**Compile**

```bash
pio run
```

**Upload**

```bash
pio run --target upload
```

```bash
pio run -t upload -e <env_name>
```

```bash
pio run -t upload -e uno
```
